---
apiVersion: velero.io/v1
kind: Restore
metadata:
  name: restore-{{ app.name }}-cluster-resources
  namespace: openshift-adp
  labels:
    velero.io/storage-location: default
  annotations:
    lca.openshift.io/apply-wave: "2"
spec:
  backupName: backup-{{ app.name }}-cluster-resources
